'use strict';

/**
 * convert-to-hdf controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::convert-to-hdf.convert-to-hdf');
